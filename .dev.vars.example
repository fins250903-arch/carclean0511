# Local secrets for `npm run cf:dev` (copy to `.dev.vars`, never commit).
# Same names as Cloudflare Workers → Settings → Variables and Secrets.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Local wrangler dev stays on http://127.0.0.1 (Worker skips HTTPS on localhost).
FORCE_HTTPS=false
