# 確認事項（回答をお願いします）

以下は自動修正できなかった／方針確認が必要な点です。回答後にCSVを再生成できます。

## Q1. Paused キーワードの空 Final URL について
Enabled の空URLはすべて埋めました。  
**Paused で Final URL が空のKWが約344件**残っています。

- A) 今は触らない（今回の方針）
- B) Paused も同じルールでRSA URLを埋める

→ 希望は A / B どちらですか？

## Q2. おもらしKWの広告グループ
`車 おもらし`（AG）内のKWは URL を `omorashi` に修正済みです。  
一方 `AG_緊急_ペット` 内の「車 おもらし シート 洗浄」も **URLのみ** `omorashi` に変更し、AGはペット緊急のままです。

- A) URL修正のみでよい（今回の方針）
- B) `車　 おもらし` AGへ移動（Removed+追加）する

→ 希望は A / B どちらですか？

## Q3. ネガティブキーワードの表記
各AGに `嘔吐""灯油""エアコン` という **Negative Phrase** があります（誤った引用符）。  
意図は「嘔吐・灯油・エアコンを除外」と思われますが、現状の1キーワードとしては機能が弱い可能性があります。

- A) 今回は触らない
- B) `嘔吐` / `灯油` / `エアコン` の3つのネガティブに分割するCSVも作る

→ 希望は A / B どちらですか？

## Q4. 群馬のKWテキスト誤記
`int群馬_通常清掃` に「車内 クリーニング 嘔吐 **大阪**」があります（今回は嘔吐AGへ移動済み）。  
キーワード文言自体の「大阪」は群馬キャンペーンでは不自然です。

- A) 文言はそのまま（Paused/Removed運用）
- B) 削除（Removedのまま追加しない）または「群馬」に書き換えたKWを追加

→ 希望は A / B どちらですか？

## Q5. Path2 の文言ニュアンス
slug別に機械割当しています（例: `kuruma-nioi-keshi`→「匂い消し」）。  
ブランドで使いたい固定文言があれば教えてください（15文字以内）。

例:
- おしっこ → 「おしっこ消臭」に変更したい など

## Q6. インポート方法
- A) 完全版フルCSVをインポートする
- B) 変更行のみをインポートする

Editorの運用に合わせてください。迷う場合は **A（フル）** を推奨します（ただし投稿前に差分確認必須）。
